@model LibraryProject.ViewModel.AddNewBook

@{
    ViewData["Title"] = "AddNew";
    //Layout = "Show";
}

<h1>AddNew</h1>

<h4>Book</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddNew">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ISNB" class="control-label"></label>
                <input asp-for="ISNB" class="form-control" />
                <span asp-validation-for="ISNB" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="ShowAllBooks">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
